---
description: Address every review comment, suggestion, issue, and annotation on the current PR.
---

Run the `respond-to-review` skill on PR `$ARGUMENTS` (or the PR for the current branch if no number is given).
